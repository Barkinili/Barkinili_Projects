*1. gün Gömülü sistem katmanları*

*2. gün git bash işlemleri*

$ git config --global user.name "[name]"

Tüm yerel depolar için kullanıcı bilgilerini yapılandırma

$ git config --global user.email "[email address]"

Tüm yerel depolar için eposta bilgilerini yapılandırma

masaüstü üzerinde bir dosya oluşturup dosya içine  readme ve txt oluşturma için gerekli kodlar 
cd ~/Desktop
mkdir proje
cd proje
touch yapilanlar.txt
touch README.md
git init
git add .
git commit -m "İlk commit"
git branch -M main
git remote add origin https://github.com/Barkinili/asisprojeler.git
git push -u origin main 

*3. gün circular buffer çalışması*
